import type { ReactNode } from 'react'; import { Link } from 'react-router-dom'; import type { Case } from '../models/workflow'; import { StatusBadge } from './common'; import { WorkflowProgress } from './WorkflowProgress';
export function CaseLayout({c,children}:{c:Case;children:ReactNode}) { return <><Link className="back" to="/cases">← Back to Cases</Link><header className="case-strip"><div><span className="eyebrow">Case detail · {c.caseRef}</span><h1>{c.beneficiaryName}</h1><p>{c.hospital}</p></div><div><StatusBadge tone={c.broadStatus.toLowerCase()}>{c.broadStatus}</StatusBadge>{c.beneficiaryNumber&&<p>Beneficiary no. {c.beneficiaryNumber}</p>}</div></header><div className="case-tabs detail-tabs"><Link to=".">Overview</Link><Link to="medical-review">Reviews</Link><Link to="sponsor-quantification">Sponsorship</Link><Link to="panel">History</Link></div><WorkflowProgress c={c}/>{children}</> }
export function ReviewStageShell({title,c,kind,children}:{title:string;c:Case;kind:string;children:ReactNode}) { const r=kind==='medical'?c.medical:kind==='social'?c.social:c.financial; return <CaseLayout c={c}><section className="screen-head"><div><span className="eyebrow">Review workspace</span><h2>{title}</h2><p>Assigned to {r.assignee}</p></div><StatusBadge tone={r.outcome.replaceAll(' ','-')}>{r.outcome}</StatusBadge></section>{children}</CaseLayout> }
